collectors:
  enabled: "cpu,memory,pagefile,logical_disk,physical_disk,net,os,service,system,time,process,textfile"
collector:
  textfile:
    directories:
      - "C:\\Program Files\\DBA Monitoring\\Prometheus_Exporter\\windows-exporter\\textfile_inputs"
  service:
    include: "^(windows_exporter|MSSQLServerOLAPService|MSOLAP\\$.+|SSASTELEMETRY.*)$"
  process:
    include: "^(msmdsrv)$"
log:
  level: error
  format: json
  file: eventlog
scrape:
  timeout-margin: 0.5
telemetry:
  path: /metrics
web:
  listen-address: ":9182"
