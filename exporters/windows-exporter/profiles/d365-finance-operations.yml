collectors:
  enabled: "cpu,memory,pagefile,logical_disk,physical_disk,net,os,service,system,time,process,tcp"
collector:
  service:
    include: "^(windows_exporter|FabricHostSvc|FabricInstallerSvc|W3SVC|WAS)$"
  process:
    include: "^(Fabric.*|Microsoft\\.Dynamics\\.AX\\..*|w3wp)$"
log:
  level: error
  format: json
  file: eventlog
scrape:
  timeout-margin: 0.5
telemetry:
  path: /metrics
web:
  listen-address: ":9182"
